I use singleton design pattern to implement the database connection, because I want to have only one connection to the database.

In the web app, I use separate language file to store the strings. So that I can easily change the language of the web app.
